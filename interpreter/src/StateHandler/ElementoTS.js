class ElementoTS {
    constructor(type, val) {
        this.val = val    // Valor del elemento
        this.type = type  // Tipo del elemento (por ejemplo, "ID", "INT", "COLOR", etc.)
    }
}

exports.ElementoTS = ElementoTS